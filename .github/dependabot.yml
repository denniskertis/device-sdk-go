version: 2
updates:
  # Maintain dependencies for Go modules
  - package-ecosystem: "gomod"
    directory: "/"
    schedule:
      interval: "daily"
    ignore:
      - dependency-name: "github.com/denniskertis/go-mod-core-contracts/v42"
        # Included when go-mod-bootstrap is updated
      - dependency-name: "github.com/denniskertis/go-mod-messaging/v42"
        # Included when go-mod-bootstrap is updated
      - dependency-name: "github.com/edgexfoundry/go-mod-registry/v3"
        # Included when go-mod-bootstrap is updated
      - dependency-name: "github.com/edgexfoundry/go-mod-secrets/v3"
        # Included when go-mod-bootstrap is updated